name: JobApplicationWizardiOS
options:
  bundleIdPrefix: com.zsparks
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

packages:
  JobApplicationWizard:
    path: ".."

targets:
  JobApplicationWizardiOS:
    type: application
    platform: iOS
    sources:
      - path: JobApplicationWizardiOS
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zsparks.JobApplicationWizardiOS
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: 1
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        SWIFT_VERSION: "5.9"
        CODE_SIGN_ENTITLEMENTS: JobApplicationWizardiOS/Resources/App.entitlements
    info:
      path: JobApplicationWizardiOS/Info.plist
      properties:
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - com.googleusercontent.apps.REDACTED_OAUTH_CLIENT_ID
    dependencies:
      - package: JobApplicationWizard
        product: JobApplicationShared
      - package: JobApplicationWizard
        product: ComposableArchitecture

  PipelineWidget:
    type: app-extension
    platform: iOS
    sources:
      - path: WidgetExtension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zsparks.JobApplicationWizardiOS.PipelineWidget
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: 1
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "5.9"
        CODE_SIGN_ENTITLEMENTS: WidgetExtension/WidgetExtension.entitlements
    dependencies:
      - package: JobApplicationWizard
        product: JobApplicationShared
      - sdk: WidgetKit.framework
      - sdk: SwiftUI.framework

  ShareJobExtension:
    type: app-extension
    platform: iOS
    sources:
      - path: ShareExtension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.zsparks.JobApplicationWizardiOS.ShareJobExtension
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: "5.9"
        CODE_SIGN_ENTITLEMENTS: ShareExtension/ShareExtension.entitlements
        INFOPLIST_KEY_CFBundleDisplayName: Save Job
    dependencies:
      - package: JobApplicationWizard
        product: JobApplicationShared
    info:
      path: ShareExtension/Info.plist
